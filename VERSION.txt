version 108
  « Dependency ':m64' requires core library desugaring to be enabled » :
  ses modules reclamaient le sucrage, et Android exigeait alors que notre
  application l'active — or c'est ce meme reglage qui declenchait l'erreur D8
  sur android.jar. On le retire donc des DEUX cotes : l'adaptateur l'efface
  dans chaque module de Mupen64Plus, fichier racine compris.
  Depuis qu'on demande Android 29, les fonctions Java recentes sont
  disponibles d'origine : cette traduction ne sert plus a rien.
